// Saldo real (hoje) de cada conta configurada em "SaldoCaixaBase" -- ver
// comentário lá para o detalhe da lógica e das chamadas à API. Depende de
// "SaldoCaixaBase" -- crie essa antes.
let
    Resultado = SaldoCaixaBase[SaldoPorConta]
in
    Resultado
