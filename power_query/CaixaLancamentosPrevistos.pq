// Lançamentos previstos "a vencer" (ainda não vencidos) nos próximos
// DiasPrevisao dias, de todas as contas configuradas -- ver "SaldoCaixaBase".
// Depende de "SaldoCaixaBase" -- crie essa antes.
let
    Resultado = SaldoCaixaBase[LancamentosPrevistos]
in
    Resultado
