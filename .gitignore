.env
__pycache__/
*.pyc
.venv/
venv/
output/*.xlsx
output/*.csv
output/*.json
debug_raw/*.json
.cache/

# Planilhas-modelo com dados financeiros reais de clientes (ex.: centria.xlsb)
# usadas como referência de layout — não devem ser versionadas.
*.xlsb
*.csv
*.xlsx

# Scripts de investigação/scratch soltos fora de sondas/ (ex.: _tmp_algo.py) —
# nunca devem ser versionados por engano num "git add -A".
_tmp_*
